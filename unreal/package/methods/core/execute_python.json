{
  "name": "execute_python",
  "domain": "core",
  "description": "Execute Unreal Editor Python code through the PythonScriptPlugin.",
  "kind": "host-backed",
  "risk": "write",
  "args": {
    "code": {
      "type": "string",
      "required": true,
      "description": "Python source code to execute inside the Unreal Editor."
    }
  },
  "returns": {
    "type": "object",
    "description": "Bridge execution result containing command_result, log output, and error details when available."
  },
  "execution": {
    "strategy": "bridge_method",
    "method": "execute_python"
  },
  "requires": {
    "bridgeMethods": [
      "execute_python"
    ]
  },
  "verify": {
    "strategy": "none"
  }
}
