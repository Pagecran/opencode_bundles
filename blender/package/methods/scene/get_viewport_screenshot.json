{
  "name": "get_viewport_screenshot",
  "domain": "scene",
  "description": "Capture the current 3D viewport and return a temp image path.",
  "kind": "host-backed",
  "risk": "read",
  "args": {
    "max_size": {
      "type": "integer",
      "description": "Optional maximum image dimension in pixels.",
      "default": 800
    }
  },
  "returns": {
    "type": "object",
    "description": "Viewport capture metadata. The plugin stores the PNG to a temp path and returns image_path."
  },
  "execution": {
    "strategy": "host_function",
    "package": "opencode_blender_bundle",
    "module": "handlers_scene",
    "function": "get_viewport_screenshot"
  },
  "requires": {
    "bridgeMethods": [
      "execute_code"
    ]
  }
}
