{
  "name": "resolve_list_fusion_tools",
  "domain": "fusion",
  "description": "List tools in the active Fusion composition or a Resolve timeline-item Fusion composition.",
  "kind": "host-backed",
  "risk": "read",
  "args": {
    "host": {
      "type": "string",
      "required": false,
      "default": "auto",
      "description": "Target host: auto, resolve, or fusion."
    },
    "timeline_item_id": {
      "type": "string",
      "required": false,
      "description": "Resolve timeline item unique id when targeting a Fusion comp on a specific timeline item."
    },
    "clip_id": {
      "type": "string",
      "required": false,
      "description": "Alias of timeline_item_id for Resolve timeline item targeting."
    },
    "timeline_item": {
      "type": "object",
      "required": false,
      "description": "Resolve timeline item selector with track_type, track_index, and item_index."
    },
    "comp_name": {
      "type": "string",
      "required": false,
      "description": "Optional Fusion comp name on the targeted Resolve timeline item."
    },
    "comp_index": {
      "type": "integer",
      "required": false,
      "default": 1,
      "description": "Optional 1-based Fusion comp index on the targeted Resolve timeline item."
    },
    "filter_type": {
      "type": "string",
      "required": false,
      "description": "Optional Fusion tool type filter, for example Merge or TextPlus."
    },
    "include_attrs": {
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Include raw attrs for each listed tool."
    },
    "max_tools": {
      "type": "integer",
      "required": false,
      "default": 100,
      "description": "Maximum number of tools to return."
    },
    "timeout_seconds": {
      "type": "number",
      "required": false,
      "default": 30,
      "description": "Maximum time to wait for the host helper."
    }
  },
  "requires": {
    "externalBinary": "python",
    "env": [
      "PAGECRAN_RESOLVE_PYTHON",
      "PAGECRAN_RESOLVE_SCRIPT_LIB",
      "PAGECRAN_FUSION_SCRIPT_LIB"
    ]
  },
  "execution": {
    "strategy": "host_cli",
    "handler": "fusion.list_tools"
  }
}
