{
  "name": "resolve_get_fusion_comp",
  "domain": "fusion",
  "description": "Inspect the active Fusion composition or a Resolve timeline-item Fusion composition.",
  "kind": "host-backed",
  "risk": "read",
  "args": {
    "host": {
      "type": "string",
      "required": false,
      "default": "auto",
      "description": "Target host: auto, resolve, or fusion."
    },
    "timeline_item_id": {
      "type": "string",
      "required": false,
      "description": "Resolve timeline item unique id when targeting a Fusion comp on a specific timeline item."
    },
    "clip_id": {
      "type": "string",
      "required": false,
      "description": "Alias of timeline_item_id for Resolve timeline item targeting."
    },
    "timeline_item": {
      "type": "object",
      "required": false,
      "description": "Resolve timeline item selector with track_type, track_index, and item_index."
    },
    "comp_name": {
      "type": "string",
      "required": false,
      "description": "Optional Fusion comp name on the targeted Resolve timeline item."
    },
    "comp_index": {
      "type": "integer",
      "required": false,
      "default": 1,
      "description": "Optional 1-based Fusion comp index on the targeted Resolve timeline item."
    },
    "include_tools": {
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Include a sample of current tools in the composition response."
    },
    "include_attrs": {
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Include raw composition attrs when available."
    },
    "max_tools": {
      "type": "integer",
      "required": false,
      "default": 50,
      "description": "Maximum number of tools to include when include_tools is true."
    },
    "timeout_seconds": {
      "type": "number",
      "required": false,
      "default": 30,
      "description": "Maximum time to wait for the host helper."
    }
  },
  "requires": {
    "externalBinary": "python",
    "env": [
      "PAGECRAN_RESOLVE_PYTHON",
      "PAGECRAN_RESOLVE_SCRIPT_LIB",
      "PAGECRAN_FUSION_SCRIPT_LIB"
    ]
  },
  "execution": {
    "strategy": "host_cli",
    "handler": "fusion.get_current_comp"
  }
}
